---
name: promo-kit
description: 把本地做好的项目（游戏/软件/工具/网站）一键变成整套对外推广物料：宣传图、宣传视频、多渠道文案、落地页 HTML、以及带完整推导过程的定价方案，并按渠道算法做差异化。宣传视频与图片中的画面直接从项目本体录制/截取再剪辑，保证与实物一致。当用户说「帮我做宣传」「给项目做推广」「生成宣传图」「做宣传视频」「录制演示视频」「写推广文案」「怎么定价」「定多少钱合适」「做张海报」「做落地页」「东西做好了没人买」「怎么卖出去」「推广我的作品」时使用。产出物全部遵守广告法与平台规则：不编销量、不造虚假稀缺、不蹭第三方 IP。本 skill 只做物料，不代发帖、不代收款、不操作任何平台后台。
agent_created: true
---

# 项目推广物料生成器

**输入一个项目目录（或 zip），输出一整套能直接用的推广物料。**

跑完你会拿到：4 张精确尺寸的宣传图（PNG，中文准确）、一套按渠道差异化的文案、
一份带推导过程的定价方案、一个可部署的落地页、一份视频分镜 + 零成本替代方案。

## 一、先划清边界

| 你要做的事 | 用哪个 skill |
|---|---|
| 做宣传、吸引人买、定价格、发到社交平台 | **本 skill（promo-kit）** |
| 在淘宝/拼多多店铺后台上架，生成可粘贴的商品标题/详情页 | `app-listing-kit` |

两者互补不重叠：**本 skill 解决「怎么让人想买」，app-listing-kit 解决「怎么挂到平台上卖」。**
如果用户最终要开店，先跑本 skill，再跑 app-listing-kit 补齐上架物料。

本 skill **不做**：代登录、代发帖、代收款、操作任何平台后台。
平台均未对个人开放商品发布 API，用脚本操作后台属违规，会封店扣保证金。

## 二、主流程

Python 一律走 managed 版本，**脚本一律加 `-u`**（不加会因缓冲丢失全部输出，已踩过）。
工作目录用绝对路径。

```bash
PY=python3          # 或你自己的 python 绝对路径
KIT="~/.workbuddy/skills/promo-kit"   # 改成你的实际安装位置

# 1) 扫描项目 → 事实清单（支持目录或 zip）
"$PY" -u "$KIT/scripts/scan_project.py" --path <项目目录或.zip> --out facts.json

# 2) 补齐事实（见第四节），可命令行覆盖
"$PY" -u "$KIT/scripts/gen_promo.py" --facts facts.json --out ./promo-out \
  --set audience=像素风怀旧玩家 --set delivery=网盘直链 --set seller_name=你的署名

# 3) 渲染宣传图（0 积分，中文准确，无需用户确认）
"$PY" -u "$KIT/scripts/render_assets.py" --dir ./promo-out

# 4) 合规复检（生成器已自动跑过一次，改完文案后必须重跑）
"$PY" -u "$KIT/scripts/lint_promo.py" --dir ./promo-out --facts facts.json
```

### 要实机宣传图 / 宣传视频时（强烈建议）

**画面必须来自项目本体。** AI 画面与实物不符，买家到手就退款。
这条链路也是 0 积分，但会启动程序并占用桌面，**动手前先跟用户说一声**。

```bash
# 5a) 全片稀疏采样，出一张联络表 —— 先看清全片再决定录什么
"$PY" -u "$KIT/scripts/capture_game.py" --project <项目目录> --out ./cap --survey
#     ★ 打开 ./cap/contact-sheet.png，挑出亮度高、信息量大的帧区间

# 5b) 按挑中的区间正式录制（同时抽出静帧供宣传图使用）
"$PY" -u "$KIT/scripts/capture_game.py" --project <项目目录> --out ./cap \
  --windows "120-260;744-828" --stills 3

# 5c) 剪辑成 16:9 与 9:16 两条宣传片（文字图层走本地渲染，中文准确）
"$PY" -u "$KIT/scripts/edit_video.py" --clips ./cap/clips --out ./宣传视频 \
  --title "<游戏名>" --cta "点击下载 · 即刻开玩" --badge "实机演示"
```

前提是项目能构建/能运行。源码里没有逐帧导出开关时会自动降级为窗口录屏；
两条路都不通就明确告诉用户「需要先能让项目跑起来」，**不要用 AI 画面顶替实机画面**。
细节与故障排查见 `references/capture-guide.md`。

退出码：`scan_project` 1 = 扫到第三方 IP 名（阻断）；`gen_promo` 1 = 含高危词；
`lint_promo` 1 = 有高危项 / 2 = 仅警告。**高危不为 0 就不要交付。**

## 三、产出物

```
promo-out/
├── 00-交付说明.md       总览 + 收款闸门结论 + 积分预算 + 手动事项
├── 01-核心文案.md       一句话定位多候选 / 三版介绍 / 卖点矩阵 / 异议处理 / CTA
├── 02-定价方案.md       三档定价 + 完整推导 + 渠道抽成对比 + 折扣纪律
├── 03-渠道文案.md       小红书/抖音/B站/闲鱼/群与朋友圈 各一套
├── 04-落地页.html       单文件零依赖，可直接部署
├── 05-素材清单.json     ImageGen/VideoGen 提示词 + 积分预算
├── 05-视频分镜.md       4 个 5 秒镜头 + 剪映步骤 + 零成本替代方案
├── 06-宣传图/*.png      ★ 已渲染好的 4 张宣传图
├── 07-宣传视频/         ★ 实机录制剪辑出的 16:9 与 9:16 宣传片（跑第 5 步才有）
├── 08-合规扫描.md       极限词/虚构数据/诱导分享/第三方 IP 扫描结果
└── promo.json           结构化数据
```

## 四、事实层从哪来（关键一步）

`scan_project.py` 只写**扫得到的东西**，扫不到的进 `missing_fields`，不要替用户假设。

它自动识别：项目名（package.json / Cargo.toml / index.html title / 目录名）、
技术栈、入口、平台、代码行数、素材体量、可用截图、README 里的定位与卖点候选。

**必须向用户追问的字段**（缺了会严重影响质量）：

| 字段 | 为什么必须要 |
|---|---|
| `audience` | 不知道给谁看，文案就没有取舍 |
| `delivery` | 买家第一顾虑是「买了怎么拿到」 |
| `seller_name` | 要在落地页与文案里署名 |
| `seller` | 收款闸门判定，见下 |

**追问方式**：一次问完，不要一轮问一个。用 `--set k=v` 直接填，或改 `facts.json`。
字段说明与完整范例见 `assets/facts.example.json`。

### 收款闸门（必须告知用户）

宣传是免费的，**但收钱需要实名**。`gen_promo.py` 会在 `00-交付说明.md` 顶部打印结论：

- 用户没提供 `seller` → 提示「宣传照常可用，但先确认走哪条收款渠道」
- 闸门未过 → 给出替代路径（监护人实名开店 / 闲鱼 / 爱发电 / 免费+捐赠）

细节见 `references/redlines.md` 第六节。**不要给做不到的方案。**

## 五、Agent 必须做的打磨（不是可选）

脚本产出的是**事实层骨架**——结构经过验证，但语言是模板化的。
`01-核心文案.md` 末尾有「给 Agent 的加工指引」，照着做：

1. **只许使用 `facts.json` 里已有的事实。** 不得新增销量、评价、用户数、奖项、
   「全网」「第一」等任何无法核实的东西。
2. 把「事实 → 意义」那列改写成买家自己的语言。
   「体积小于 30MB」→「下载几秒钟，老笔记本也放得下」。
3. 每条读一遍问自己：**买家看完知道买到了什么吗？** 不知道就重写。
4. 写完**重跑 `lint_promo.py`**，高危清零。
5. 中文文案里不得出现任何第三方作品名 / IP 名。

改完把 `03-渠道文案.md`、`04-落地页.html` 一起同步改掉，然后重跑第 4 步。

## 六、素材生成：先报预算，再动手

**宣传图已经生成好了，0 积分。** AI 生成是可选项，而且**要花积分**：

| 素材 | 单件消耗 | 数量 | 合计 |
|---|---|---|---|
| ImageGen 单张 | 约 5–10 积分 | 2–3 | 15–30 |
| VideoGen 5 秒 | 约 50–100 积分 | 4 | 200–400 |

**调用 ImageGen / VideoGen 前，必须把这张预算表念给用户听并得到同意。**
不打招呼就花掉几百积分，是不可逆的损失。

推荐给用户的顺序：**先用零成本海报投出去看数据 → 有反馈再花积分**。
P1 最小试水（1 张图 + 1 条视频）约 55–110 积分。

**AI 不写字。** 所有文字由本地浏览器渲染。AI 只做无文字的氛围视觉，
提示词结尾固定加 `no text, no letters, no words, no watermark`。
详见 `references/visual-specs.md`。

### 但实机素材不该花积分

**项目自己的画面一律从实机录（第 5 步），不要用 ImageGen / VideoGen 生成 —— 转 0 积分，
而且不会出现"图和实物不符"。** AI 只用在实机拍不到的地方：概念氛围图、频道头图背景。
有了第 5 步，上表里 VideoGen 那 200–400 积分通常根本不需要花。

## 七、硬规则

1. **不编造数据。** 没有销量就不写销量，没有评价就不写评价。
2. **不造虚假稀缺。**「限时」「仅剩 N 份」做不到就不要写。
3. **不蹭第三方 IP。** 项目里、文案里、提示词里都不能出现他人作品名。
   描述效果就描述效果本身。
4. **不诱导分享。** 转发/集赞/助力在微信生态是明确违规。
5. **平台规则会变。** 字数限制、保证金、抽成均为参考值，
   交付时必须注明「以后台当前实际要求为准」。
6. **讲清取舍。** 自动做的选择（定价、配色、平台假设、档位名）要在
   `00-交付说明.md` 里列出来并注明可改。
7. **画面必须是真的。** 宣传图与宣传视频里的界面一律来自项目实机录制，
   不得用 AI 画面、他人素材或预渲染动画冒充。剪辑也不能拼出游戏里没有的效果。
   `--bgm` 只能传自有或已授权的音乐。

## 八、参考文档（按需加载，不要一次全读）

| 文件 | 何时读 |
|---|---|
| `references/redlines.md` | **总是先读**。极限词、虚构数据、IP 红线、收款闸门、AI 合规 |
| `references/visual-specs.md` | 生成/调整宣传图与视频时读。双轨制、规格表、API 参数、渲染坑 |
| `references/channel-playbook.md` | 打磨渠道文案时读。各平台算法逻辑与有效句式 |
| `references/pricing-playbook.md` | 讨论定价或用户质疑价格时读。锚定原理、心理价位、调价信号 |
| `references/capture-guide.md` | 要录实机画面或剪宣传视频时读。两条录制路线的选择、游戏侧开关怎么加、本机 ffmpeg 的硬限制与相应对策 |

## 九、常见故障

| 现象 | 原因与解法 |
|---|---|
| 脚本没任何输出就结束 | 忘了加 `-u`，stdout 被缓冲后丢失。加 `-u` 重跑 |
| 宣传图尺寸/内容不对 | 复用了 user-data-dir。`render_assets.py` 已每张换新，别手动改 |
| 找不到浏览器 | 脚本会提示降级方案（手动打开 `06-宣传图/src/*.html` 整页截图） |
| 合规扫描报假阳性 | 词库是无脑匹配的。像「这不是盗版」这种防御性表述也会被命中——**改措辞，不要改扫描器**，因为平台过滤同样无脑 |
| 定价看起来偏高 | 先看 `02-定价方案.md` 的推导表，逐项改 `--set`。体量系数按代码行数估算，不适用时直接覆盖 |
| 项目扫出来没有截图 | 跑第 5 步自动录：`capture_game.py --survey` 出联络表，挑段后正式录制并抽静帧。**AI 画面不能代替实机截图** |
| 录不到画面 / 找不到可执行文件 | 先确认项目能构建能跑。无头导出要求源码有 `DEBUG_CAPTURE` 开关，没有就自动降级窗口录屏。别用 AI 画面顶替 |
| 录出来整体偏暗 | 整片色调本来就暗。先 `--survey` 出联络表，按亮度标注挑亮段，别均匀抽帧 |
| 宣传片里文字是乱码/方块 | 文字图层必须走本地浏览器渲染再 overlay。本机 ffmpeg 没有 `drawtext`，也别指望它烧中文字幕 |
| 竖版视频把游戏画面裁掉了 | 用模糊背景补边，不要 `crop` 主体。见 `capture-guide.md` 第五节 |
